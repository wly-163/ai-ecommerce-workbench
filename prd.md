## AI电商智慧运营工作台 — 完整PRD与落地计划


### 一、项目概述

**项目名称**：AI电商智慧运营工作台（E-Commerce AI Ops Workbench）

**一句话定位**：一个低代码AI工作流编排平台，通过3D数据可视化与多智能体协作，为电商企业提供智能导购、智能客服、供应链预测、营销内容生成四大核心能力。

**目标用户**：
- **运营人员**：通过低代码界面配置AI规则，无需写代码
- **数据分析师**：使用工作流编排复杂的数据分析任务
- **管理者**：通过3D数据大屏直观掌握业务全局

**商业价值**：
- 智能导购：提升客单价与转化率
- 智能客服：大模型驱动的AI客服渗透率已达72%，意图识别准确率超93%
- 供应链预测：实现“一店一策”差异化选品，补货决策从经验驱动转向数据驱动
- 营销内容：7×24小时AI直播与自动化内容生成


### 二、四大核心场景与功能模块

#### 场景一：AI智能导购与个性化推荐

**用户故事**：用户打开电商聊天窗口，输入“帮我搭一套去海边度假、防晒、拍照好看的衣服”，AI理解意图后推荐商品组合并附上购买链接。

**功能点**：
| 功能 | 描述 | 优先级 |
|------|------|--------|
| 多模态意图识别 | 理解用户自然语言中的款式、场景、预算等复合意图 | P0 |
| RAG商品检索 | 从商品知识库中召回相关商品，经CrossEncoder重排序 | P0 |
| LLM推荐生成 | 基于检索结果生成个性化推荐话术与搭配建议 | P0 |
| 多轮对话管理 | 支持用户追问、修改条件、对比商品 | P1 |
| 会话记忆 | 跨会话记住用户偏好、尺码、历史购买 | P1 |

**技术实现**：意图识别Agent → RAG检索（向量+BM25双路召回）→ Reranker精排 → LLM生成推荐


#### 场景二：智能客服与用户体验优化

**用户故事**：用户输入“我昨天买的鞋码数不对，想换货”，AI自动查询订单、判断换货资格、引导用户完成换货流程。

**功能点**：
| 功能 | 描述 | 优先级 |
|------|------|--------|
| 意图路由 | 将用户问题路由至售前/售后/退换货等专业Agent | P0 |
| 订单/物流查询 | 调用业务API查询订单状态、物流轨迹 | P0 |
| RAG政策检索 | 从售后政策知识库检索退换货规则 | P0 |
| 幻觉检测与自纠正 | G-Eval风格检测回答忠实度，不达标则重新生成 | P1 |
| Human-in-the-Loop | 高风险请求（如大额退款）自动转人工审核 | P1 |
| WebSocket实时同步 | 用户与管理员界面实时状态更新 | P1 |

**技术实现**：intent_router → presales_agent / aftersales_agent → response_review


#### 场景三：供应链智能预测与补货

**用户故事**：运营打开工作台，系统自动推送“商品A预计3天后断货，建议补货500件”，并生成补货清单。

**功能点**：
| 功能 | 描述 | 优先级 |
|------|------|--------|
| 销量预测 | 基于历史销量、天气、促销等多因素预测未来销量 | P0 |
| 库存预警 | 实时监控库存水位，低于安全线自动告警 | P0 |
| 智能补货清单 | 自动生成“一店一策”差异化补货建议 | P0 |
| 选品分析 | 分析各品类销售趋势，推荐新品/淘汰品 | P1 |
| 采购单自动生成 | 补货决策通过API自动生成采购订单 | P2 |

**技术实现**：数据接入 → 时序预测模型 → 规则引擎判断 → 补货清单生成 → 3D场景联动


#### 场景四：AI营销内容生成

**用户故事**：运营输入商品链接和营销目标，AI自动生成直播脚本、带货短视频和多语言商品文案。

**功能点**：
| 功能 | 描述 | 优先级 |
|------|------|--------|
| 直播脚本生成 | 基于商品详情生成带互动话术的直播脚本 | P0 |
| 多语言文案 | 一键生成多语种商品标题与描述 | P1 |
| 数字人推流 | 脚本+TTS+视频合成 → RTMP推流至直播平台 | P2 |
| 智能剪辑 | 自动剪辑直播高光片段用于二次传播 | P2 |

**技术实现**：商品数据抓取 → LLM脚本生成 → TTS语音合成 → FFmpeg推流


### 三、技术架构

#### 整体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        前端层 (React 19 + TS)                    │
├───────────────┬───────────────┬───────────────┬─────────────────┤
│  3D场景编辑器  │  工作流编辑器  │  运营仪表盘   │  聊天界面(客服/导购)│
│ (Three.js/R3F)│ (React Flow)  │ (ECharts)    │ (shadcn/ui)     │
└───────┬───────┴───────┬───────┴───────┬───────┴────────┬────────┘
        │               │               │                │
        └───────────────┴───────────────┴────────────────┘
                                │ WebSocket / SSE / REST API
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                      API网关层 (FastAPI)                        │
├───────────────┬───────────────┬───────────────┬─────────────────┤
│  认证/鉴权    │  工作流管理   │  任务调度     │  文件上传/管理   │
│  (JWT)       │  (CRUD)      │  (Celery)    │  (Multipart)    │
└───────┬───────┴───────┬───────┴───────┬───────┴────────┬────────┘
        │               │               │                │
        ▼               ▼               ▼                ▼
┌───────────────┐ ┌───────────────┐ ┌───────────────┐ ┌───────────────┐
│  AI编排引擎   │ │  RAG引擎      │ │  业务工具层   │ │  异步任务队列  │
│  (LangGraph)  │ │ (ChromaDB)    │ │ (API调用)    │ │  (Celery+Redis)│
└───────────────┘ └───────────────┘ └───────────────┘ └───────────────┘
        │               │               │                │
        └───────────────┴───────────────┴────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                     数据层 (PostgreSQL)                         │
│  用户表 │ 工作流定义表 │ 订单表(模拟) │ 商品表(模拟) │ 知识库块表 │
└─────────────────────────────────────────────────────────────────┘
```

#### 技术栈明细

| 层级 | 技术选型 | 版本/说明 |
|------|----------|-----------|
| **前端框架** | React + TypeScript + Vite | React 19, Vite 5 |
| **UI组件** | shadcn/ui + Tailwind CSS | 现代、可定制 |
| **状态管理** | Zustand + TanStack Query | 轻量+服务端状态 |
| **3D引擎** | Three.js + React Three Fiber + Drei | 3D场景渲染 |
| **工作流编辑器** | React Flow (@xyflow/react) | 拖拽式DAG编辑器 |
| **图表** | ECharts + Recharts | 数据可视化 |
| **后端框架** | FastAPI | Python异步，自动生成API文档 |
| **AI编排** | LangGraph 1.2+ | 有状态多智能体工作流 |
| **AI框架** | LangChain 1.x | 与LangGraph配合 |
| **向量数据库** | ChromaDB (开发) / Milvus (生产) | 本地轻量/企业级 |
| **Embedding** | BAAI/bge-small-zh-v1.5 | 中文效果好 |
| **LLM** | DeepSeek API / 通义千问 | 国内可用，成本低 |
| **Reranker** | Qwen3-VL-Rerank | 精排提升召回质量 |
| **异步任务** | Celery + Redis | 耗时任务处理 |
| **数据库** | PostgreSQL + SQLModel | ORM与类型安全 |
| **实时通信** | WebSocket | 状态同步 |
| **容器化** | Docker + Docker Compose | 一键启动 |
| **部署** | Vercel (前端) + Railway (后端) | 免费额度可用 |


### 四、分阶段落地计划（10周）

#### 第一阶段：基础设施与MVP（第1-3周）

**目标**：跑通“前端3D场景 + 后端AI工作流”的最简闭环

| 周次 | 任务 | 产出 | 验收标准 |
|------|------|------|----------|
| 第1周 | 项目初始化、前后端框架搭建、Docker配置 | 可运行的空项目 | `docker-compose up` 能启动所有服务 |
| 第2周 | 实现基础LangGraph工作流（输入→处理→输出） | 一个可执行的简单DAG | 能通过API调用工作流并返回结果 |
| 第3周 | 前端3D场景搭建（Three.js基础场景+轨道控制） | 可交互的3D场景 | 场景中有立方体，可旋转/缩放 |

**第1周具体任务**：
1. 创建GitHub仓库 `ai-ecommerce-workbench`
2. 初始化前端：`pnpm create vite frontend --template react-ts`
3. 初始化后端：`poetry new backend`
4. 编写 `docker-compose.yml` 编排postgres、redis、backend、frontend
5. 第一个commit：项目骨架

**第2周具体任务**：
1. 定义LangGraph State与Nodes
2. 实现一个“商品推荐”工作流：输入query → 模拟检索 → LLM生成推荐
3. 用FastAPI封装 `/api/workflows/execute` 接口
4. 支持SSE流式输出

**第3周具体任务**：
1. 使用 `@react-three/fiber` 创建3D场景
2. 添加 `OrbitControls` 实现相机控制
3. 放置代表“商品”的3D立方体（不同颜色代表不同品类）
4. 前端调用后端API，用返回结果改变立方体颜色


#### 第二阶段：核心功能开发（第4-7周）

**目标**：实现四大场景的核心能力

| 周次 | 任务 | 产出 | 验收标准 |
|------|------|------|----------|
| 第4周 | AI智能导购（意图识别+RAG检索+LLM生成） | 可对话的导购机器人 | 输入“推荐防晒衣”能返回商品推荐 |
| 第5周 | 智能客服（意图路由+订单查询+政策RAG） | 可处理售前/售后问题的客服 | 输入“查订单”能返回订单状态 |
| 第6周 | 工作流编辑器（React Flow拖拽+连线+配置面板） | 低代码DAG编辑器 | 能拖拽节点、连线、保存工作流 |
| 第7周 | 供应链预测（销量预测API+库存预警+3D联动） | 预测与预警系统 | 3D场景中库存低的商品变红 |

**第4周具体任务**：
1. 实现多模态意图识别（用LLM Function Calling分类用户意图）
2. 搭建ChromaDB，导入模拟商品数据
3. 实现向量检索 + BM25双路召回
4. 用LangGraph编排“意图识别→检索→重排序→生成推荐”工作流
5. 前端实现聊天界面（shadcn/ui）

**第5周具体任务**：
1. 实现intent_router节点，区分售前/售后/意图不明
2. 实现presales_agent（商品查询+库存查询）
3. 实现aftersales_agent（订单查询+售后政策）
4. 实现response_review节点，防止幻觉
5. 搭建模拟订单数据（PostgreSQL）

**第6周具体任务**：
1. 集成React Flow到前端
2. 预置节点：Input、LLM、RAG、Condition、Output
3. 实现节点拖拽、连线、删除
4. 实现节点配置面板（点击节点弹出配置）
5. 保存工作流定义 → 后端存储

**第7周具体任务**：
1. 实现销量预测API（可用简单回归模型或LLM推理）
2. 实现库存预警规则引擎
3. 3D场景中：正常=绿色，预警=黄色，缺货=红色
4. 定时任务（Celery Beat）自动触发预测


#### 第三阶段：炫技功能与体验优化（第8-9周）

**目标**：让项目从“能用”变为“惊艳”

| 周次 | 任务 | 产出 | 验收标准 |
|------|------|------|----------|
| 第8周 | 3D场景增强（动态数据绑定、流动线条、粒子效果） | 炫酷的3D数据大屏 | 数据变化时3D场景平滑过渡 |
| 第9周 | 多智能体协作 + 可观测性 | 完整的Agent链路追踪 | 能看到每个节点的输入/输出/耗时 |

**第8周具体任务**：
1. 实现3D对象与后端数据的动态绑定
2. 用流动线条模拟“物流路径”
3. 用粒子效果表现“流量”或“热度”
4. 实现场景切换（仓库视角/销售视角/物流视角）
5. 暗黑模式 + 响应式设计

**第9周具体任务**：
1. 扩展LangGraph为多Agent协作（导购Agent、客服Agent、供应链Agent）
2. 实现可观测性：每个节点的输入/输出/耗时/Tokens用量
3. 前端实现“执行追踪”面板，可视化展示工作流执行过程
4. 实现Human-in-the-Loop（高风险操作转人工确认）


#### 第四阶段：部署与文档（第10周）

**目标**：让招聘官点开链接就能体验

| 任务 | 产出 | 验收标准 |
|------|------|----------|
| 编写Dockerfile | 前后端各自容器化 | `docker-compose up` 一键启动 |
| 部署到云端 | 可公开访问的Demo | Vercel + Railway 在线地址 |
| 撰写README | 高质量项目文档 | 含架构图、GIF演示、快速开始 |
| 撰写技术博客 | 1-2篇踩坑记录 | 发布到掘金/知乎 |
| 录制演示视频 | 3分钟场景演示 | 展示四大场景完整流程 |

**README必须包含**：
1. 项目背景与四大场景说明
2. 技术架构图（用Draw.io绘制）
3. 3分钟演示GIF/视频
4. 一键启动命令：`docker-compose up`
5. 技术决策说明（为什么选LangGraph？为什么用Chroma？）


### 五、关键里程碑与交付物

| 里程碑 | 时间 | 交付物 |
|--------|------|--------|
| M1: MVP上线 | 第3周末 | 3D场景 + 可调用的AI工作流 |
| M2: 核心功能完成 | 第7周末 | 四大场景全部可演示 |
| M3: 炫技功能完成 | 第9周末 | 多Agent协作 + 可观测性 + 3D增强 |
| M4: 正式发布 | 第10周末 | v1.0.0 发布 + 部署上线 + 博客发表 |


### 六、GitHub仓库结构

```
ai-ecommerce-workbench/
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   │   ├── scene/          # 3D场景组件 (Three.js/R3F)
│   │   │   ├── workflow/       # 工作流编辑器 (React Flow)
│   │   │   ├── chat/           # 聊天界面 (客服/导购)
│   │   │   └── dashboard/      # 运营仪表盘 (ECharts)
│   │   ├── stores/             # Zustand状态
│   │   ├── api/                # API调用 (TanStack Query)
│   │   └── App.tsx
│   ├── package.json
│   └── Dockerfile
├── backend/
│   ├── app/
│   │   ├── api/                # FastAPI路由
│   │   ├── core/
│   │   │   ├── workflow.py     # LangGraph工作流定义
│   │   │   ├── nodes.py        # 各节点实现
│   │   │   ├── tools.py        # 业务工具(订单查询等)
│   │   │   └── rag.py          # RAG检索逻辑
│   │   ├── models/             # SQLModel数据模型
│   │   ├── services/           # 业务服务层
│   │   └── celery_app.py       # 异步任务
│   ├── pyproject.toml          # Poetry依赖
│   └── Dockerfile
├── docker-compose.yml
├── README.md
├── WHITEPAPER.md               # 技术决策白皮书
└── .github/
    └── workflows/              # CI/CD (可选)
```


### 七、立即行动清单（今天就开始）

1. **创建GitHub仓库**
   ```bash
   gh repo create ai-ecommerce-workbench --public
   git clone https://github.com/你的用户名/ai-ecommerce-workbench
   ```

2. **初始化项目结构**
   ```bash
   mkdir ai-ecommerce-workbench && cd ai-ecommerce-workbench
   mkdir frontend backend
   ```

3. **写第一个commit**
   - 创建 `README.md`，写下项目愿景
   - 创建 `docker-compose.yml` 骨架

4. **今晚的目标**：完成项目初始化，push到GitHub
